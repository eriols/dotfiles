[global]
font = Iosevka Term 11

# Markup handling
markup = yes

# Format (markup allowed)
format = "<b>%s</b>\n%b"

# Sort messages by urgency.
sort = no

# Show how many messages are currently hidden (because of geometry/space).
indicate_hidden = yes

# Alignment of message text: left, center, right
alignment = center

# Scrolling / wrapping
show_age_threshold = -1
word_wrap = yes
ignore_newline = no

# Duplicates
stack_duplicates = yes
hide_duplicate_count = yes

# ---- Placement / sizing ----
origin = top-right
offset = (15, 49)
width = 300
# Optional pixel cap; leave commented if you want it to grow as needed.
# height = 400

# Shrink window if it's smaller than the width. Ignored if width is 0.
shrink = no

# Transparency [0..100] (requires compositor)
transparency = 5

# Idle behavior
idle_threshold = 0

# Monitor selection
monitor = 0
follow = none

# History
sticky_history = yes
history_length = 15

# Indicators for URLs/actions
show_indicators = no

# Layout
line_height = 3
separator_height = 2
padding = 6
horizontal_padding = 6
separator_color = frame

# External programs
dmenu = /usr/bin/dmenu -p dunst:
browser = /usr/local/bin/firefox --new-tab

# Icons
icon_position = left
max_icon_size = 80
icon_path = "/home/olsson/code/dotfiles/dunst/icons/"

# Frame
frame_width = 3
frame_color = "#8EC07C"

# ---- Shortcuts (moved from deprecated [shortcuts] section) ----
close = ctrl+space
close_all = ctrl+shift+space
history = ctrl+grave
context = ctrl+shift+period

[urgency_low]
frame_color = "#3B7C87"
foreground = "#3B7C87"
background = "#191311"
timeout = 4

[urgency_normal]
frame_color = "#5B8234"
foreground = "#5B8234"
background = "#191311"
timeout = 6

[urgency_critical]
frame_color = "#B7472A"
foreground = "#B7472A"
background = "#191311"
timeout = 8

# ---- Rules ----

[slack-firefox]
appname = "Firefox"
summary = "*Slack*"
new_icon = "/home/olsson/code/dotfiles/dunst/icons/SLA-Logo-Mark-Black-RGB.png"
timeout = 20
background = "#EFF8FD"

[Spotify]
appname = "Spotify"
format = "<b>Now Playing:</b>\n%s\n%b"
new_icon = "/home/olsson/code/dotfiles/dunst/icons/Spotify-Icon-Logo.wine.png"
history_ignore = yes
timeout = 4
urgency = low
